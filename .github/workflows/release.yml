# Tag vX.Y.Z on main (or workflow_dispatch for artifacts only).
# Thin orchestration: real logic lives in scripts/ci/ (GHA/Bamboo agnostic).

name: Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-sdist-wheel:
    name: sdist and wheel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Build distributions
        run: python scripts/ci/build_dist.py
      - name: Upload dist artifacts
        uses: actions/upload-artifact@v7
        with:
          name: colosseum-dist
          path: dist/*
          if-no-files-found: error

  build-offline-wheelhouse:
    name: offline (${{ matrix.os }}, py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Build offline wheelhouse zip
        run: python scripts/ci/build_offline_wheelhouse.py --zip
      - name: Upload offline wheelhouse
        uses: actions/upload-artifact@v7
        with:
          name: offline-${{ matrix.os }}-py${{ matrix.python-version }}
          path: colosseum-core-offline-*.zip
          if-no-files-found: error

  build-docs:
    name: documentation (HTML + PDF)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: python -m pip install -U pip setuptools wheel && python -m pip install -e ".[docs,test]"
      - name: Cache LaTeX apt archives
        uses: actions/cache@v5
        with:
          path: .cache/apt/archives
          key: latex-release-ubuntu-${{ hashFiles('.github/workflows/release.yml') }}-v2
          restore-keys: latex-release-ubuntu-
      - name: Install LaTeX toolchain
        run: |
          mkdir -p "$PWD/.cache/apt/archives/partial"
          sudo apt-get update
          sudo apt-get -o Dir::Cache::archives="$PWD/.cache/apt/archives" install -y latexmk texlive-latex-recommended texlive-fonts-recommended texlive-latex-extra zip
          sudo chown -R "$USER":"$USER" "$PWD/.cache/apt"
          rm -rf "$PWD/.cache/apt/archives/partial"
      - name: Build documentation
        run: python scripts/docgen/build_all.py
      - name: Archive HTML site
        run: |
          cd build/docgen/site/html
          zip -r "${GITHUB_WORKSPACE}/colosseum-docs-html.zip" .
      - name: Upload documentation artifacts
        uses: actions/upload-artifact@v7
        with:
          name: colosseum-docs
          path: |
            build/docgen/site/latex/*.pdf
            colosseum-docs-html.zip
          if-no-files-found: error

  publish:
    name: publish GitHub release
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    needs: [build-sdist-wheel, build-offline-wheelhouse, build-docs]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Verify tag matches pyproject version
        run: python scripts/ci/verify_release_tag.py

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-assets

      - name: Flatten release files
        run: |
          mkdir -p release-files
          find release-assets -type f \( \
            -name '*.whl' -o -name '*.tar.gz' -o -name '*.pdf' -o -name '*.zip' \
            \) -exec cp -t release-files {} +
          echo "Release assets:"
          ls -la release-files

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Colosseum Core ${{ github.ref_name }}
          generate_release_notes: true
          body: |
            ## Install (online)

            ```bash
            pip install colosseum-core
            ```

            Or install the `.whl` / sdist attached below.

            ## Offline wheelhouses

            Use the zip matching your OS and Python (3.9 or 3.12):

            - `colosseum-core-offline-linux-py*.zip`
            - `colosseum-core-offline-windows-py*.zip`

            ```bash
            unzip colosseum-core-offline-<os>-py<ver>.zip -d wheelhouse
            python -m pip install --no-index --find-links=wheelhouse colosseum-core
            ```

            GUI requires Tk in the Python install (Windows python.org OK; Linux bake `python3-tk`).

            Documentation: PDF and `colosseum-docs-html.zip`.
          files: release-files/*
          fail_on_unmatched_files: true
