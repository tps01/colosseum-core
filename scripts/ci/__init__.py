"""CI-agnostic helpers (GitHub Actions, Bamboo, local).

Entry points under this package directory:

- ``build_dist.py`` — sdist + wheel
- ``build_offline_wheelhouse.py`` — offline zip with third-party deps
- ``verify_release_tag.py`` — tag vs pyproject version check
"""
