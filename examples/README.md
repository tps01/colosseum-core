# Examples

Core contains the runtime, decorators, plugin registry, runners, and evidence storage.
Bench and device examples belong to the plugin repository that implements their APIs.

[`plugins/colosseum_template`](plugins/colosseum_template/) is the maintained example
for creating a standalone plugin package.
